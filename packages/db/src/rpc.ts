/**
 * Typed RPC wrappers for Supabase Edge Functions.
 * Stubs for now — populated as Edge Functions are created in Phase 3.
 */

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface RpcFunctions {
    // Will be populated in Phase 3:
    // admin_create_outlet: { args: {...}, returns: {...} }
    // admin_assign_role: { args: {...}, returns: {...} }
}
